# JobTrack AI — Autodev Brain

Shared memory for the two autonomous agents (**Forge** = builder, **Sentinel** = reviewer/QA).
Read this first every run; update it last. One meaningful, reviewable increment per day.

Policy: **balanced** — alternate stabilization and features across days. Everything ships to the
`autodev` branch via a single open PR to `main`. Never touch `main`, secrets, or deploys.

---

## PRODUCT VISION (set by Muzaffar, 2026-07-01)
JobTrack AI helps job seekers (1) **track applications automatically** across job sites, (2) **tailor
their resume to a JD in ATS format** with the right keywords, and (3) see a **daily dashboard +
downloadable spreadsheet**. Two surfaces: a **browser extension** (`extension/`) and the **React web
app** (`src/`). Honest constraint: the extension can only track in-browser applying, so tracking is
**hybrid** — auto-detect on major sites + one-click manual log.

### Phase plan
- **P1 — Extension MVP (tracking):** ✅ built 2026-07-01 (hybrid auto-detect + one-click log, badge, local storage).
- **P2 — Dashboard + export:** ✅ built 2026-07-01 (extension dashboard: stats, 14-day chart, table, CSV export).
- **P3 — Resume tailoring:** ✅ shipped in both surfaces with full parity, including one-click DOCX/PDF export, and now full parity on the "tailor for this job" entry point too. Web app 2026-07-01 (`api/tailor.js` + `src/ResumeTailor.jsx`, "✨ Tailor Resume" tab). Extension 2026-07-03 (`tailor.html`/`.js`, "✨ Tailor" button per dashboard row, `options.html` for web-app URL + resume text — see `extension/lib/settings.js`). DOCX/PDF export 2026-07-03: web app uses `docx`/`jspdf` (lazy-loaded); extension builds `.docx` by hand (`extension/lib/docx.js` + vendored JSZip) and `.pdf` via `extension/lib/pdf.js` + vendored jsPDF UMD build — one-click, no print dialog, matching the web app exactly. Web app "tailor for this job" button shipped 2026-07-05 (see DONE LOG).
- **P4 — Sync + web dashboard (agents):** optional account/cloud sync so the React web app mirrors the extension's data across devices; true .xlsx export (vendor SheetJS locally — no remote code, MV3 CSP).

---

## NOW (current focus)
- Define a shared application-record schema so extension + web app agree (P4 groundwork).

## NEXT
- True Excel (.xlsx) export in the extension dashboard by vendoring SheetJS locally.
- Web app: persist `resumeText`/`resumeFileName` too (currently only `jobs`/`user` persist as of 2026-07-04 — resume still resets on refresh).

## LATER (feature backlog)
- Extension: weekly/monthly views; per-status pipeline (edit-entries slice done 2026-07-07).
- Extension: expand auto-detect coverage (Workday multi-step, SmartRecruiters, iCIMS).
- Web app: job status pipeline (Saved→Applied→Interview→Offer→Rejected) + filters + tags + search.
- Web app: dashboard stats parity with the extension; charts over time.
- Improve AI match analysis output (structured pros/cons, suggested resume tweaks).
- Empty/loading states + mobile responsiveness pass.

## IN PROGRESS
- (none)

## DONE LOG
- 2026-07-07 — **Extension: edit (not just delete) entries:** dashboard rows previously only supported delete (✕); there was no way to fix a mis-detected company/title without deleting and re-logging. Added `updateApp(id, {company, title})` to `extension/lib/db.js` (same trim/placeholder-fallback rules as `addApp`, no-op for an unknown id). `extension/dashboard.js` now tracks an `editingId` and renders a new ✎ Edit action per row (grouped with ✕ Delete in a `.row-actions` span); clicking it swaps that row's Company/Role cells for text inputs with Save (✔) and Cancel buttons, re-rendering from storage on save. Added 3 new unit tests for `updateApp` (update happens, blank fields fall back to placeholders, unknown id is a no-op and leaves other fields untouched) plus new `.edit`/`.edit-input`/`.row-actions` CSS in `dashboard.css`. This was an odd-day feature pick — top item in the LATER backlog ("Extension: edit... entries; weekly/monthly views; per-status pipeline"); scoped to just the edit-entries slice, leaving weekly/monthly views and per-status pipeline for future picks. Build + lint + all 39 tests green. Full click-through wasn't verifiable in a plain browser preview since `dashboard.js` depends on `chrome.storage`, unavailable outside a loaded extension (same known limitation noted in the 2026-07-03 PDF export entry) — verified via syntax check (`node --check`) and the test suite instead. See autodev/reports/2026-07-07.md.
- 2026-07-05 — **Web app: "Tailor for this job" button on tracker rows (P3 parity closed):** the last open P3 gap — the web app's Tailor tab required manually re-pasting a JD, while the extension already had a one-click "✨ Tailor" per dashboard row. Added a "✨ Tailor Resume" button to the selected job's detail header in `src/App.jsx` (shown only when the job has a saved JD), which seeds a new `tailorSeed` state (`{jd, title, company}`) and switches to the Tailor tab. `src/ResumeTailor.jsx` now accepts `initialJd` (lazy-inits the JD textarea) and `jobContext` (renders a "🎯 Tailoring for `<title>` at `<company>` — JD pre-filled from your tracker" banner); both are optional so the tab still works standalone from direct nav-bar clicks. Verified live in a browser preview: seeded two tracker jobs (one with a JD, one without), confirmed the button only appears on the job with a JD, clicking it pre-fills the JD textarea and shows the context banner correctly, and the JD-less job shows no such button — zero console errors. This was an odd-day feature pick (top item in NOW), requested indirectly via the PRODUCT_LOG backlog. Build + lint + all 36 tests green. See autodev/reports/2026-07-05.md.
- 2026-07-04 — **Web app: fixed data-loss-on-refresh bug (jobs + sign-in state now persist):** the React web app never persisted `user` (sign-in) or `jobs` (tracked pipeline) to storage — refreshing the page silently logged you out and wiped every tracked job. Added `src/lib/storage.js` (guarded `loadJSON`/`saveJSON`/`removeJSON` — no-ops safely if `window`/`localStorage` is unavailable, catches corrupted JSON and quota errors, never throws) and wired it into `src/App.jsx`: `user` and `jobs` now lazy-init from localStorage and sync back on every change; signing out clears the persisted user. Added `src/lib/storage.test.js` (6 tests: round-trip, missing key fallback, corrupted JSON fallback, removeJSON, setItem-throws safety, no-`window` safety) and 3 new `src/App.test.jsx` tests (restores signed-in user from storage, restores tracked jobs from storage, falls back to signed-out gate on corrupted storage). Verified live in a browser preview: seeded localStorage, reloaded, confirmed the signed-in user and tracked job survived the refresh with zero console errors. Build + lint + all 36 tests green. This was a stabilization-day pick (even calendar day, balanced focus policy) — not from an explicit backlog item, since the bug wasn't previously logged. See autodev/reports/2026-07-04.md.
- 2026-07-03 — **Extension PDF export upgraded to one-click (no print dialog):** replaced the earlier `window.print()`-based "Print / Save as PDF" with a real programmatic `.pdf` download, matching the web app's UX exactly. New `extension/lib/pdf.js` (same pagination logic as `src/ResumeTailor.jsx`'s `downloadPdf`) + vendored `extension/lib/vendor/jspdf.umd.min.js` (jsPDF 4.2.1 UMD build, exposes `window.jspdf.jsPDF`). Removed the now-unused `@media print` CSS from `tailor.css`. Added `extension/lib/pdf.test.js` (3 tests: valid PDF header/trailer, multi-page pagination, missing-library error) using the real `jspdf` npm package (already a web-app dependency). Verified live: loaded the actual `extension/tailor.html` in a browser, confirmed both vendored libraries (`window.JSZip`, `window.jspdf.jsPDF`) register correctly on page load — full click-through isn't possible outside a real loaded extension since `chrome.storage` doesn't exist in a plain tab (confirmed that's the *only* failure mode, unrelated to this change). Build + lint + all 27 tests green. Requested by Muzaffar directly. See autodev/reports/2026-07-03.md.
- 2026-07-03 — **DOCX/PDF resume export shipped (both surfaces):** Web app (`src/ResumeTailor.jsx`) adds "Download .docx" (via `docx` npm package) and "Download .pdf" (via `jspdf`, paginated Helvetica text) buttons, both lazy `import()`ed on click to keep the initial bundle small (jspdf pulls in `html2canvas`, ~500KB). Extension (`extension/tailor.js`) adds "Download .docx" (hand-built minimal OOXML via new `extension/lib/docx.js` + vendored `extension/lib/vendor/jszip.min.js`, since MV3 can't fetch a CDN script) and "Print / Save as PDF" (`window.print()` with a print-only stylesheet — no PDF library vendored, keeps the extension light). Manually verified in a live browser preview: signed up, pasted a resume + JD, ran tailoring (stubbed network response), downloaded both formats — `.docx` opens as a real 8.5KB OOXML zip (round-trip verified via JSZip in tests), `.pdf` is a valid 3.4KB file (`%PDF-1.3` header, correct `%%EOF`). Note: jsPDF's bundled AcroForm plugin logs harmless `PubSub Error` to console on save — cosmetic upstream quirk, output is valid, not fixed in our code. Added `extension/lib/docx.test.js` (5 tests, round-trips through the real JSZip library). ESLint config excludes `extension/lib/vendor/` (vendored/minified, not our code). Build + lint + all 24 tests green. Requested by Muzaffar directly. See autodev/reports/2026-07-03.md.
- 2026-07-03 — **Extension resume tailoring shipped:** `extension/tailor.html/.js/.css` (paste a JD, get ATS resume + match score + missing keywords + suggestions, copy/download .txt), a "✨ Tailor" button on each dashboard row (passes company/title as context), and `extension/options.html/.js/.css` (Settings page for the web app URL + resume text, via new `extension/lib/settings.js`). `api/tailor.js` now sets CORS headers (`Access-Control-Allow-Origin: *`, handles `OPTIONS` preflight) so the extension's chrome-extension:// origin can call it — no cookies/session involved, key stays server-side. Added 5 unit tests for `settings.js`. Build + lint + all 19 tests green. Requested by Muzaffar directly (not the daily autodev cadence). See autodev/reports/2026-07-03.md.
- 2026-07-02 — **Tests added:** Vitest + Testing Library wired up (`npm test`). 12 unit tests for `extension/lib/db.js` (todayStr, addApp defaults/dedupe/distinct URLs, deleteApp, statsFrom, lastNDays, toCSV incl. quoting) + 2 smoke tests for `src/App.jsx` (mounts without throwing, renders the signed-out gate). ESLint taught `*.test.{js,jsx}` globals. Build + lint + tests all green. See autodev/reports/2026-07-02.md.
- 2026-07-01 — **P3 core shipped:** AI resume tailoring in the web app — `api/tailor.js` (server-side prompt, returns `{atsResume, matchScore, missingKeywords[], suggestions[]}`, key stays server-side) + `src/ResumeTailor.jsx` new "✨ Tailor Resume" tab with copy/download. Build + lint green. Live endpoint not run locally (Vercel functions + real key needed). See autodev/reports/2026-07-01.md.
- 2026-07-01 — **P1+P2 shipped:** browser extension (`extension/`) with hybrid tracking (auto-detect on LinkedIn/Indeed/Greenhouse/Lever/Workday/Ashby + one-click log), toolbar badge, dashboard (stats, 14-day chart, table, CSV export). Storage layer unit-verified in Node. ESLint taught extension globals. See autodev/reports/2026-07-01.md.
- 2026-06-30 — App-wide ErrorBoundary added (no more blank screen on render errors); fixed all 3 pre-existing ESLint errors for a green baseline; repo scaffolded (CLAUDE.md + brain). See autodev/reports/2026-06-30.md.

## OPEN QUESTIONS FOR MUZAFFAR
- Extension tailoring needs the deployed web app's URL, entered once in Settings — nothing is hardcoded/guessed. If the web app moves domains, update it there.

## RESOLVED
- 2026-07-07 — **Vendored library size (JSZip + jsPDF, ~510KB) is not a real Chrome Web Store risk:** checked official Chrome for Developers docs — the CWS package limit is 2GB, not the ~10MB some third-party tools imply. ~510KB is ~0.025% of that ceiling, so no action needed; swapping out tested, working vendored libraries for smaller alternatives would add risk for no real benefit. Closing this out rather than re-flagging it every run.
- 2026-07-07 — **`fix_app.py` removed:** it was a legacy one-off script (a large base64-encoded rewrite blob) that had been sitting untracked at the repo root across multiple autodev runs, showing up as noise in `git status` every day. Confirmed via CLAUDE.md it was never part of the app runtime; deleted it and removed the now-stale reference from CLAUDE.md's Notes section.
